# Client demo runtime defaults (Vite)
# Copy this file to .env and edit for your environment.
SIGNALING_SERVER=https://localhost:8096

# Should match the daemon's BROWSER_HEADLESS setting. Informational only: lets the
# client page know whether the daemon's Puppeteer browser runs headless, which is
# relevant for headless-only dynamic click calibration.
BROWSER_HEADLESS=true
CLIENT_STATIC_HOST=0.0.0.0
CLIENT_STATIC_PORT=80

# Serve the client demo over HTTPS. When true, CLIENT_STATIC_TLS_CERT and
# CLIENT_STATIC_TLS_KEY must point at PEM files (paths are resolved relative to
# the client package root). Leave false to serve plain HTTP.
CLIENT_STATIC_HTTPS=false
# CLIENT_STATIC_TLS_CERT=certs/client.crt
# CLIENT_STATIC_TLS_KEY=certs/client.key
# Optional CA chain and key passphrase.
# CLIENT_STATIC_TLS_CA=certs/ca.crt
# CLIENT_STATIC_TLS_PASSPHRASE=

# Optional ICE/STUN/TURN configuration for browser P2P
# Use comma or newline separated URL lists.
# STUN_SERVER_URLS=stun:example.com:3478
# TURN_SERVER_URLS=turn:example.com:3478?transport=udp,turn:example.com:3478?transport=tcp
# TURN_USERNAME=username
# TURN_CREDENTIAL=password
# Or provide the full OWT/WebRTC shape directly.
RTC_ICE_SERVERS_JSON=[{"urls":"stun:example.com:3478"},{"urls":["turn:example.com:3478?transport=udp","turn:example.com:3478?transport=tcp","turns:example.com:443?transport=tcp"],"username":"","credential":""}]
